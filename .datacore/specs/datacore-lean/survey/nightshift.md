# Survey: nightshift execution (2026-09-23)

Read-only subagent survey; suspicions cite lines and are UNVERIFIED until modelled or replayed.

1. **Task lifecycle × attempt fence × stalled GC × queue resolution** (modules/nightshift/lib/claim.py:498-553, task_attempt.py:14-42, execute.py:655-690, requeue_stalled.py:38-186, task_queue.py:542-755) — claim.py:513-517 says a failed attempt stays NEXT and the SECOND consecutive failure escalates to REVIEW. Suspicions: (a) timeouts/exceptions/unacked runs write fence `unknown:` (task_attempt.py:38-39) and post-success failures keep `completed`, so the NEXT task is fenced out forever by `delegation_status` (task_queue.py:105-107, 724-734) — never reaches the second failure; (b) NIGHTSHIFT_ATTEMPTS never reset, so not "consecutive"; (c) escalation writes `NIGHTSHIFT_OUTPUT: ''` (claim.py:548) which matches GC's "REVIEW without output → stalled" (requeue_stalled.py:47-48) → requeued up to MAX_REQUEUES=2 (~4 executions); (d) queue entries re-admit REVIEW sources with NEXT state (task_queue.py:584, 599-603) unboundedly; (e) no supported re-run of a task with output (reconcile_attempt.py:149-153). No test for the escalation. Model M.
2. **Claim/release in-flight marker** (claim.py:379-465, run.py:1568-1704, requeue_stalled.py:52-63) — STARTED written before claim failure returns (claim.py:410-411 vs 423-458) and run.py:1570-1572 `continue`s without stamping COMPLETED; pre-hook abort same (1584-1589) ⇒ task invisible 6h then requeued with REQUEUES+1: transient push failure burns the escalation cap. Model S–M.
3. **Evaluator consensus gate** (evaluate.py:160-369) — unknown persona (164-171) and unparseable exit-0 output (score 0.5, 247-256) counted as real votes; first `score:\s*([\d.]+)` clamped to 1.0 so "score: 8" = max approval → DONE; configured quality_threshold 0.80 never read (thresholds hard-coded 296-310). Model S.
4. **delegation_canary verdict** (lib/delegation_canary.py:90-199) — `blocked` returns 0 regardless of age (176-178; test pins 99h blocked passes); missing RESULT → `unknown`, later checks return 0 (171-183); `git add` failure ignored (148-152). Model S.
5. **gstack postprocess gate** (postprocess.py:135-367) — looks correct now; cheap regression proof "pass ⇒ both available ∧ both passed". Model S.
6. **In-run retry** (run.py:1268, 1706-1763) — single `if` ⇒ at most one retry for any max_retries ≥ 1; NIGHTSHIFT_RETRIES never persisted; quota on retry marked failed vs deferred on first attempt. No tests. Model S.
7. **Daily budget cap** (run.py:1057-1107, execute.py:404-731) — reporting only; `api_fallback_allowed` ignored; batch API spends regardless. Low current risk (budget 0 = unlimited). Model S.
8. **jobs/recurrence counter** (lib/jobs/recurrence.py:88-200) — lock-unavailable fallback updates unlocked (119-125); `_save` swallows OSError (97-98): a reset can be lost. Model S.
9. **ai_task_gate vs executor** (lib/ai_task_gate.py:40-100 vs nightshift_parser.py:36-47, 379) — claims to mirror exactly; differs on ROADMAP requirement, case of `unassigned`, and `"AI" in tags` vs `startswith('AI')`. Model S.
10. **cadence_liveness contract line** (lib/cadence_liveness.py:95-229) — unreadable venture.yaml / cadence log skipped silently ⇒ "0 cadence(s) overdue". Model S.
11. **workflow_executor** (lib/workflow_executor.py:220-387) — LIVE mode skips phases yet reports `completed`; no runtime callers. Model S.
12. **jobs/awake sleep accounting** (lib/jobs/awake.py:136-281) — looks correct; cheap proof 0 ≤ awake_age ≤ wall_age. Model S.

Not ranked: quality_gate.py (KB dedup), verifier_replay.py (one-off), nightshift_recover_stranded.py (glue; stale origin), repair_nightshift_org.py (bypasses @serialized writer; no evidence it runs), queue_ai_tasks.py, undelegate_unexecutable.py.
