# Survey: research module, agents, detectors, briefing, context (2026-09-23)

Read-only subagent survey; suspicions cite lines and are UNVERIFIED until modelled or replayed.

1. **rotate_learning_backup "keep last 7 per file"** (lib/rotate_learning_backup.py:54-81) — glob `f"{stem}-*{suffix}"` for `engrams` also matches `engrams-candidates-*` backups, which sort after `engrams-2026…` ('c' > '2'), so every `engrams.yaml` backup is purged. Evidence reported on disk: state/learning-backups/_root/ has 0 `engrams-2026*.yaml` vs 7 candidates backups. Also 1-second timestamp resolution (66-70) overwrites same-second rotations. No tests. Model S.
2. **detectors/actor_presence MISSING/STALLED/SILENT** (lib/detectors/actor_presence.py:164-268) — (a) log with no readable events stored as `{space: None}` → reported ok, contradicting :17; (b) deletion in one space while another space still has the log is never detected (loop :202, baseline overwritten :264); (c) STALLED rewrites baseline (:263) so it self-heals next run. No tests. Model S–M.
3. **research queue lifecycle** (modules/research/lib/research_orchestrator.py:86-175, 687-868, 1054-1117) — (a) analysis failure (:1071-1074) never increments attempts → head-of-line blocking; (b) items identified by heading text (756, 761, 709) → duplicate titles misdirect CLOSED/:OUTPUT:; (c) `:END:` search (771) scans 15 lines past next heading; (d) heading not found → silent unchanged rewrite, reprocessed next run. Model M.
4. **today_registry stage plan/validator** (lib/today_registry.py:80-272) — cross-stage dependency on a LATER stage treated as satisfied (163-164; rule 5 at 245-249 never checks stage order); unknown stage coerced to "gather" (100). Model S.
5. **briefing/actions dedupe "dismissed means gone forever"** (lib/briefing/actions.py:61-177) — normalisation only folds case/whitespace (72), so "no matter how the briefing rephrases it" (14-15) is false; check-then-act without lock (133). Model S.
6. **detectors/seq_gap classification** (lib/detectors/seq_gap.py:101-268) — local None with remote present prints "ok … published" exit 0 (247); head_seq = last parseable line not max (108-117); local<remote clamped to 0 (251). Model S.
7. **context_merge DIP-0002 layering** (lib/context_merge.py:32-53, 319-423) — no override, pure concatenation (347-370); validation hit only warns, still writes (421); `.space.md` never validated (41); rebuild_all writes composed CLAUDE.md incl. PRIVATE without checking gitignore (450). Latent, not observed. Model S.
8. **reliability_scoreboard R5 / streak** (lib/reliability_scoreboard.py:203-337) — `hits >= expected - max(1, expected//200)` (225) always tolerates 1 miss: 3/4 = 75% passes "≥99.5%"; <4 expected passes vacuously (223-224); streak reads file order not date order; `--date` backfill appends out of order (334-336). Model S.
9. **detectors/id_churn "only growth above it"** (lib/detectors/id_churn.py:47-147) — count-based not set-based (115): k repaired + k new churn = 0 growth. Model S.
10. **prune_learning_buffer** (lib/prune_learning_buffer.py:166-342) — index takes every `statement:` incl. retired engrams (184); kept entries mutated by `rstrip("-")` (329). No tests. Model S.
11. **agent_stream_store idempotent append** (lib/agent_stream_store.py:18-96) — no counterexample; cheap proof target (append idempotence, batch atomicity). Model S.
12. **decay_and_review month window** (lib/decay_and_review.py:48-55) — "last N months including current" via `now - 30*i days`: on the 31st returns 2 distinct months; Mar 31 → {Mar, Jan}, Feb skipped. No tests. Model S.

Lower priority: research_router scoring (looks correct), knowledge_commit append-only prefix (well tested, L), datacore/ledger.py, pending_decisions, session_time_guardian (claims hold / trivial).
