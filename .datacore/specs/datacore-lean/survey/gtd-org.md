# Survey: GTD + org-mode core (2026-09-23)

Read-only subagent survey; items marked VERIFIED were reproduced in memory by the surveyor. Others are UNVERIFIED until modelled or replayed.

1. **Adapter state change vs ledger dismissal** (lib/org_workspace_adapter.py:201-226, 394-438, 940-1029; org_workspace workspace.py:257-320) — DIP-0009:338-341 "DONE and CANCELLED both dismiss". (a) `transition` on a repeater task returns early (task stays TODO, SCHEDULED advanced, workspace.py:298-306) but cmd_complete emits `item.dismiss` unconditionally (431) → habits (0-personal/org/habits.org, 27 repeaters) permanently dismissed in the ledger; (b) `update --state DONE|CANCELLED` emits only `item.update` (1010-1019) → ledger item stays open; used by gtd_decision_board.py:577-580. Model S.
2. **DIP-0009 v2.0 transitions vs code** (org_workspace/_types.py:40-120) — code allows any non-terminal → any state in sequence incl. retired QUEUED/WORKING/FAILED. VERIFIED: can_transition True for DEFERRED→DONE, DEFERRED→NEXT, REVIEW→TODO, REVIEW→WAITING, TODO→QUEUED, NEXT→FAILED; "owner only" never checked; env_keys puts DEFERRED in todo class vs canonical header done class; DIP self-contradiction at :462-466. Model S.
3. **org_transaction journal/recovery** (lib/org_transaction.py:92-338) — (a) watched destination losing the rename race leaves the journal entry → recover raises RecoveryRequired → every later serialized call blocked (global stuck lock); (b) stale-write check vacuous when file first watched inside write() (151-156); (c) many tools write org files directly without lock/journal: org_dedup_within_file.py:160, org_resolve_id_conflicts.py:73, inbox_dedup.py, triage_utils.py, stamp_seq_todo.py, org_date_hook.py:41, validate_org_dates.py --fix, org_union_merge.py --apply. Model M.
4. **org_union_merge** (lib/org_union_merge.py:43-101) — VERIFIED: (a) `block not in a` (95) is a substring test: theirs "** TODO call mom" dropped when ours contains "*** TODO call mom"; (b) theirs-only child appended at EOF → reparented under last heading. Model S.
5. **Day-of-week auto-fix hook / date_utils** (lib/org_date_hook.py:13-41 — LIVE PostToolUse hook; date_utils.py:34, 111-145; org_date_validator.py:24-95) — VERIFIED: unbracketed pattern without `\b` rewrites "2026-09-24 Monitor" → "2026-09-24 Thuitor", "2026-08-27 Wednesday" → "Thunesday" in every .org/.md after each Edit/Write; `parse_relative("next month")` returns next Monday; org_date_validator.py:68 hard-codes 2025 as suspect; validate_org_dates FM_* regexes run over whole file (40-41, 88-93). Model S.
6. **inbox_cleanup** (lib/inbox_cleanup.py:43-155) — CLOSED includes DEFERRED (44) so wakeable DEFERRED items are archived out of the live set (DIP-0009:330-337) and never wake; disagrees with org_archive_closed.py:20; star lines inside #+begin_ blocks counted as headings. Runs nightly via gtd_hygiene.py:114. Model M.
7. **org_dedup_within_file** (lib/org_dedup_within_file.py:56-162) — identity() strips :ID:/:DISPATCH_ID: (51-52, 138) so copies with DIFFERENT ids are "identical" and one deleted (ledger holds both ids); plain write_text (160); only top-level siblings. No tests. Model S.
8. **org_resolve_id_conflicts** (lib/org_resolve_id_conflicts.py:33-74) — always keeps HEAD (70), which is upstream only in a rebase; "nothing references it" false for add-minted ids (ledger item.create); direct write. No tests. Model S.
9. **dedup_tasks.retire_duplicate** (lib/dedup_tasks.py:36-100) — CANCELLED transition on a repeater advances date instead; no ledger event (Phase-1 projection reverts); tie-break sort is dead code (82 vs 85); open children under CANCELLED parent. Model S.
10. **task_cleanup dup-ids** (lib/task_cleanup.py:111-205) — new ids `f'{tid}-copy{i+1}'` (134) not UUID and not unique across reruns → next load refuses files, stopping hourly cycle; meridian swap mutates canonical; no ledger notify. No tests. Model S.
11. **org_archive_closed** (lib/org_archive_closed.py:43-128) — no suspicion; positive target / reference spec for #6. Model M.
12. **triage_utils._append_task_body** (150-205) — duplicate test searches whole file (188); inserts at first `:END:` after heading without boundary (186); _CLOSED_STATES omits DEFERRED (149); direct write. **dedup.deduplicate** (106-122) — Jaccard not transitive, drops C when only B~C. Model S.

Checked, no issue: ID minting (adapter:753-767), stamp_seq_todo (idempotent), org_literal, priority_score, org_guard (advisory), gtd module JS tools (glue).
