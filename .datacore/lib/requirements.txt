# Datacore core dependencies
pyyaml>=6.0
org-workspace>=0.5.1   # main's pin kept: 0.5.1 was published 2026-08-10 to fix 7 dead nightshift services; the branch's >=0.4.4 would regress that
cryptography>=50.0.0  # includes current certificate, curve-validation and bundled OpenSSL fixes
packaging>=24.0       # verify runtime versions against declared requirements
markdown-it-py==4.2.0  # source-mapped Markdown sections; preserve literal examples
ruamel.yaml>=0.18     # registry_validate.py and preserved_yaml.py: round-trip YAML that keeps comments

# Module deps that were never declared and so were never installed. Both
# modules failed silently for weeks: news served 13-day-old cached headlines
# (feedparser missing since ~2026-08-04) and the /today voice post-hook died
# at the TTS step (gtts missing). Homebrew python is PEP-668 managed, so they
# live in .datacore/venv — see venv_site_packages() in venv_bootstrap.py.
feedparser>=6.0   # news module: feed_fetcher.py
gTTS>=2.5         # voice-terminal: speak_brief.py fallback when kokoro_onnx is absent
youtube-transcript-api>=1.2.4,<1.3  # transcript reader uses the instance/list API
yt-dlp>=2026.8.19  # transcript reader's metadata/playlist subprocess

# Calendar sync (optional - needed for /today calendar integration)
# google-api-python-client>=2.0
# google-auth-oauthlib>=1.0

# Semantic search / Datacortex (optional)
# sentence-transformers>=2.0
# chromadb>=0.4

# Development / testing
# pytest>=7.0
